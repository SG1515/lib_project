<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kcc.lib_project.domain.book.mapper.BookMapper">

    <insert id="insertBook" parameterType="BookVo">
        insert into book(isbn, title, author, publisher, publishPredate, page, kdc, contents, bookIndex, imageUrl)
        values(#{isbn}, #{title}, #{author}, #{publisher}, #{publishPredate}, #{page}, #{kdc}, #{contents}, #{bookIndex}, #{imageUrl})
    </insert>

</mapper>




