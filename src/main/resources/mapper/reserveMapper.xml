<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kcc.lib_project.domain.reserve.mapper.ReserveMapper">

    <insert id="createReserve" parameterType="com.kcc.lib_project.domain.reserve.vo.ReserveVo">
        INSERT INTO reserve
        (reserve_id, reserved_at, call_number, user_id, status)
        VALUES (reserve_seq.NEXTVAL, SYSDATE, #{callNumber}, #{userId}, #{status})
    </insert>

    <select id="isReserved" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM reserve WHERE call_number = #{callNumber} AND status = 'WAITING'
    </select>


</mapper>




